<Window x:Class="AdminClientWpf.Views.DashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:AdminClientWpf.ViewModels"
        xmlns:core="clr-namespace:AdminClientWpf.Core"
        Title="DnD Admin • Dashboard" Width="1200" Height="780"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource Bg0Brush}"
        x:Name="RootWindow">
    <Window.Resources>
        <core:WindowWidthToBoolConverter x:Key="WidthToCompactConverter"/>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="{StaticResource CardBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="RowBackground" Value="{StaticResource Bg1Brush}"/>
            <Setter Property="AlternatingRowBackground" Value="{StaticResource CardBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Gold2Brush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{StaticResource Bg1Brush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="BorderBrush" Value="{StaticResource Gold2Brush}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>

        <Style x:Key="SidebarLabelStyle" TargetType="TextBlock">
            <Setter Property="Visibility" Value="Visible"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding ElementName=RootWindow, Path=ActualWidth, Converter={StaticResource WidthToCompactConverter}, ConverterParameter=980}" Value="True">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SidebarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bd" Property="Background" Value="{StaticResource Bg0Brush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ThemeToggleStyle" TargetType="ToggleButton">
            <Setter Property="Width" Value="64"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Grid>
                            <Border x:Name="Track" CornerRadius="16" Background="#36244F"/>
                            <Ellipse x:Name="Thumb" Width="22" Height="22" Fill="{StaticResource GoldBrush}" HorizontalAlignment="Left" Margin="4,0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="#E5D8F7"/>
                                <Setter TargetName="Thumb" Property="Fill" Value="#5D4A2B"/>
                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                                <Setter TargetName="Thumb" Property="Margin" Value="0,0,4,0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Row="0" Grid.ColumnSpan="2" Background="{StaticResource Bg1Brush}" BorderBrush="{StaticResource Gold2Brush}" BorderThickness="0,0,0,1" Padding="20,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Admin dashboard" Style="{StaticResource TitleText}"/>
                    <TextBlock Margin="16,6,0,0" Foreground="{StaticResource SubTextBrush}" Text="Felhasználó kezelés"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Margin="0,0,10,0" Foreground="{StaticResource SubTextBrush}" Text="Dark"/>
                    <ToggleButton Style="{StaticResource ThemeToggleStyle}" IsChecked="{Binding IsLightTheme}"/>
                    <TextBlock Margin="10,0,0,0" Foreground="{StaticResource SubTextBrush}" Text="Light"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Grid.Column="0" Background="{StaticResource Bg1Brush}" BorderBrush="{StaticResource Gold2Brush}" BorderThickness="0,0,1,0">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Width" Value="240"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ElementName=RootWindow, Path=ActualWidth, Converter={StaticResource WidthToCompactConverter}, ConverterParameter=980}" Value="True">
                            <Setter Property="Width" Value="72"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Grid Margin="16,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel>
                    <TextBlock Text="⚔ DnD Tool" FontSize="18" FontWeight="SemiBold"/>
                    <TextBlock Margin="0,4,0,0" Style="{StaticResource SidebarLabelStyle}" Foreground="{StaticResource SubTextBrush}" Text="Admin panel"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Margin="0,24,0,0">
                    <Button Style="{StaticResource SidebarButtonStyle}" Content="👤 Users"/>
                    <Button Style="{StaticResource SidebarButtonStyle}" Content="📊 Reports" IsEnabled="False"/>
                    <Button Style="{StaticResource SidebarButtonStyle}" Content="⚙ Settings" IsEnabled="False"/>
                </StackPanel>

                <StackPanel Grid.Row="2">
                    <TextBlock Style="{StaticResource SidebarLabelStyle}" Foreground="{StaticResource SubTextBrush}" Text="{Binding AdminLabel}"/>
                    <Button Margin="0,12,0,0" Content="Logout" Command="{Binding LogoutCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Grid.Column="1" Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,18">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Vertical">
                    <TextBlock Text="Users" FontSize="20" FontWeight="SemiBold"/>
                    <TextBlock Margin="0,6,0,0" Foreground="{StaticResource SubTextBrush}" Text="Admin-only user management"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="Refresh" Width="120" Margin="0,0,12,0" Command="{Binding RefreshCommand}"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" CornerRadius="16" Background="{StaticResource CardBrush}" BorderBrush="{StaticResource Gold2Brush}" BorderThickness="1" Padding="12">
                    <DataGrid ItemsSource="{Binding Users}"
                              SelectedItem="{Binding SelectedUser}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="160" IsReadOnly="True"/>
                            <DataGridComboBoxColumn Header="Role"
                                                    SelectedItemBinding="{Binding Role, UpdateSourceTrigger=PropertyChanged}"
                                                    ItemsSource="{Binding DataContext.RoleOptions, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    Width="120"/>
                            <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive, UpdateSourceTrigger=PropertyChanged}" Width="90"/>
                            <DataGridTextColumn Header="Created" Binding="{Binding CreatedAt, StringFormat=yyyy-MM-dd}" Width="120" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Last login" Binding="{Binding LastLoginAt, StringFormat=yyyy-MM-dd HH:mm}" Width="160" IsReadOnly="True"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>

                <StackPanel Grid.Column="1" Margin="18,0,0,0">
                    <Border CornerRadius="16" Background="{StaticResource CardBrush}" BorderBrush="{StaticResource Gold2Brush}" BorderThickness="1" Padding="16">
                        <StackPanel>
                            <TextBlock Text="Selected user" FontSize="18" FontWeight="SemiBold"/>
                            <TextBlock Margin="0,10,0,0" Foreground="{StaticResource SubTextBrush}" Text="{Binding SelectedUser.Email}"/>
                            <TextBlock Margin="0,4,0,0" Foreground="{StaticResource SubTextBrush}" Text="{Binding SelectedUser.Username}"/>

                            <StackPanel Margin="0,18,0,0">
                                <TextBlock Text="Role" Style="{StaticResource HintText}"/>
                                <ComboBox ItemsSource="{Binding RoleOptions}" SelectedItem="{Binding SelectedUser.Role}" Margin="0,6,0,0"/>
                            </StackPanel>

                            <StackPanel Margin="0,14,0,0">
                                <CheckBox Content="Active" IsChecked="{Binding SelectedUser.IsActive}"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,18,0,0">
                                <Button Content="Update role" Width="140" Margin="0,0,12,0" Command="{Binding UpdateRoleCommand}"/>
                                <Button Content="Update status" Width="140" Command="{Binding UpdateStatusCommand}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <Border Margin="0,16,0,0" CornerRadius="16" Background="{StaticResource Bg1Brush}" BorderBrush="{StaticResource Gold2Brush}" BorderThickness="1" Padding="14">
                        <StackPanel>
                            <TextBlock Foreground="{StaticResource DangerBrush}"
                                       Text="{Binding ErrorMessage}"
                                       TextWrapping="Wrap"
                                       Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                            <TextBlock Foreground="{StaticResource GoldBrush}"
                                       Margin="0,6,0,0"
                                       Text="{Binding StatusMessage}"
                                       TextWrapping="Wrap"
                                       Visibility="{Binding HasStatus, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <Grid Grid.ColumnSpan="2" Background="#80000000" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Border CornerRadius="18" Background="{StaticResource Bg1Brush}" BorderBrush="{StaticResource Gold2Brush}" BorderThickness="1" Padding="20" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <StackPanel>
                            <TextBlock Text="Loading data..." FontSize="16" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                        <StackPanel Margin="0,16,0,0">
                                <Rectangle Height="14" RadiusX="6" RadiusY="6" Fill="{StaticResource Bg0Brush}"/>
                                <Rectangle Height="14" Margin="0,10,0,0" RadiusX="6" RadiusY="6" Fill="{StaticResource Bg0Brush}"/>
                                <Rectangle Height="14" Margin="0,10,0,0" RadiusX="6" RadiusY="6" Fill="{StaticResource Bg0Brush}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>
        </Grid>

        <ItemsControl Grid.Row="0" Grid.RowSpan="2" Grid.Column="0" Grid.ColumnSpan="2" ItemsSource="{Binding Toasts}" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,90,24,0">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Margin="0,0,0,10" Padding="14,10" CornerRadius="12" BorderBrush="{StaticResource Gold2Brush}" BorderThickness="1" Background="{StaticResource CardBrush}">
                        <TextBlock TextWrapping="Wrap">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsError}" Value="True">
                                            <Setter Property="Foreground" Value="{StaticResource DangerBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                            <TextBlock.Text>
                                <Binding Path="Message"/>
                            </TextBlock.Text>
                        </TextBlock>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Window>
